# apps/web — Angular Frontend (placeholder)

This directory will contain the Angular frontend for EffortEstimator.

To scaffold the project:

```bash
cd apps/web
ng new effort-estimator-web --routing --style=scss --skip-git
```
